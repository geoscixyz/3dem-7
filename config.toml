# The URL the site will be built for
base_url = "https://example.com"

# Title of the website
title = "3DEM"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[extra]
# Put all your custom variables here

# [extra.sponsors]

    # [extra.sponsors.simpeg]
    # image = "simpeg.png"
    # alt_text = "SimPEG"
    #
    # [extra.sponsors.other1]
    # image = "simpeg.png"
    # alt_text = "Other1"
    #
    # [extra.sponsors.other2]
    # image = "simpeg.png"
    # alt_text = "Other2"

sponsors = [
    {image="simpeg.png", alt_text="SimPEG", url="https://www.simpeg.xyz"},
    {image="simpeg.png", alt_text="Other1", url="https://duckduckgo.com/"},
    {image="simpeg.png", alt_text="Other2", url="https://duckduckgo.com/"},
]
