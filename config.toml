# The URL the site will be built for
base_url = "https://example.com"

# Title of the website
title = "3DEM"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[extra]

# Additional information about the workshop
sponsors = [
    {image="simpeg.png", alt_text="SimPEG", url="https://www.simpeg.xyz"},
    {image="simpeg.png", alt_text="Other1", url="https://duckduckgo.com/"},
    {image="simpeg.png", alt_text="Other2", url="https://duckduckgo.com/"},
]

navlinks = [
    {name="Registration", url="/#registration"},
    {name="Call for Abstracts", url="/#abstracts-call"},
    {name="Previous editions", url="https://www.gwhohmann.org/", external=true},
]

when = "November 13th to 15th, 2023"
where = [
    "Earth, Ocean and Atmospheric Sciences",
    "University of British Columbia",
    "2207 Main Mall, Vancouver, BC",
]

registrations = [
    {type="Early bird", fee="XX"},
    {type="Regular", fee="XX"},
    {type="Student", fee="XX"},
]
